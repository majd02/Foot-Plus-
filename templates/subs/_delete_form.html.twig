<form method="post" action="{{ path('subs_delete', {'numSub': sub.numSub}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub.numSub) }}">
    <button class="btn">Delete</button>
</form>
